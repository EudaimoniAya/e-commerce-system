---
description: 测试替身规范——替身放哪（放置连续谱）+ 替身叫什么（Meszaros 五类命名）+ 反模式禁令。AI 写测试替身时必须遵守。
globs: tests/**/*.py,app/**/*.py
alwaysApply: false
---

# 测试替身规范（Test Doubles）

> 测试替身（Test Double）指"代替真实依赖的假对象"——比如测试里用一个假 LLM 代替真实 DeepSeek 调用。本规范回答三个问题：**替身放哪、叫什么、怎么用**。
> 本文件只含**长期规范**（约束"以后怎么写"）。一次性迁移动作（改名/删除/升级当前代码）见对应 change 的迁移清单，**不写进本文件**——迁移完成即失效，避免规范出现过期信息。
> 决策来源：放置与命名规范（ADR-015）；历史反模式实证见 git `128c555`（环境变量劫持清理）。

## 〇、替身的作用与边界（为什么用替身）

开发范式为 SDD+BDD（规格在 OpenSpec 文档），**替身不承担"提示开发者要实现什么"的功能**（那是文档的事）。替身只剩三个作用、三条边界：

**作用**
1. **受控输入**：给被测代码喂测试控制的已知输入（比如固定返回 `{"intent": "unknown"}`），让断言可复现——真实 LLM 每次输出不同，无法稳定断言
2. **隔离外部**：把无法在本地跑起来的外部依赖（LLM、短信、邮件这类外部服务）隔离在测试之外——测试要高频执行（CI 每次、开发时频繁），真实调用要花钱、慢、结果不确定、还依赖网络
3. **验证被测代码自身的逻辑**：测试的目的是"知道代码错在哪"，错的对象是**被测代码本身**（它的路由、参数传递、分支判断），不是它调用的外部服务

**边界（替身不干什么）**
1. **不提示实现**：替身不做设计提示——"要实现什么"由 OpenSpec 规格承担
2. **不验证真实对接**：真实服务的返回格式、行为是否兼容，由协议层测试（用 httpx.MockTransport 模拟 HTTP）+ E2E 真实链路验证；**替身全绿 ≠ 真实链路绿**
3. **不评质量**：答得好不好（效果质量）由 RAGAS 评测

### 概念澄清：替身 ≠ 假实现 ≠ 测试环境

判断一个对象算不算"替身"，看它的**角色**（在测试里代替协作者，还是在生产里运行），不是看它**假不假**：

| | 测试里用（注入给被测代码） | 生产里用（产品运行路径上） |
|---|---|---|
| **假实现**（行为是假的/简化的） | **替身**（Test Double）——测试注入的假 LLM、假短信 | **生产组件**（模式/占位实现）——系统默认用的假实现、还没接真实网关的产品实现 |
| **真实现**（真实组件） | **测试环境**（真实依赖的测试实例）——devbox 起的测试库、Testcontainers 起的容器 | 正常生产组件——真实 HTTP 客户端 |

- **测试库不是替身**：它是真实 PostgreSQL 的独立实例（真实现 + 测试里用）——"切环境"（开发/测试/生产用不同数据库）≠ 替身；"切真假"（用 SQLite 替代 PostgreSQL）才是替身
- 本规范管的是**假实现**这一类（放哪 + 叫什么），替身只是其中"测试里用"的子集；生产侧的假实现也归本规范管（档 2-5），但**不叫替身，叫生产组件**

## 一、放置连续谱：替身放哪（五档）

替身的摆放位置是一个连续谱，从"全在测试里"到"进生产代码"，共五档（档 = 摆放方式，数字只是快速指代）：

| 档 | 摆放方式 | 评价 |
|---|---|---|
| 1 | 测试自行全部装配（替身全在 tests/，测试文件内自造） | ⚠️ 合法但**冗余**：和产品侧假实现功能重复，优先复用 |
| 2 | **双模式**：产品里同时有假/真两个实现，配置字段（如 `provider`）选择用哪个，装配层统一分派 | ✅ **首选**：配置看得见、可验证、可只换一个组件；必须带快速失败（生产环境选到假实现时启动即报错） |
| 3 | 环境级连坐：一个环境变量（如 `app_env`）同时切换一批组件（开发环境全部用假实现） | ✅ 合法：假实现多时省事，但选择是隐式的（不知道具体哪个在假） |
| 4 | 生产代码硬编码调用假实现（没有协议、没有抽象） | ❌ **不规范**：换真实现要改业务代码 |
| 5 | 测试劫持生产行为（业务代码里加"测试专用"的环境变量分支） | ❌ **反模式**：同一个函数两条行为路径，测试验证的那条生产根本不会走（历史案例见反模式禁令） |

### 三个判断问题（决定用哪档）

1. **这个依赖能在本地跑起来吗？**（测试环境能否起一个真实的）
   - 能（数据库、Redis——开发环境能起真库）→ 测试用**真实依赖**，不需要替身
   - 不能（LLM、短信、邮件——外部服务）→ 看问题 2
2. **除了测试，还有谁需要这个假实现？**（"不配外部凭据也能跑"是**可选的产品决策**，不是必然）
   - 选了"开箱即用"（本地开发/演示不用配 key）→ 档 2 或 3（生产侧假实现 + 配置切换）
   - 没选（所有环境都配凭据）→ 档 1（假实现全在 tests/，测试自己组装）
   - ⚠️ **CI 需要假实现不算"还有谁需要"**——CI 就是跑测试的地方，等于测试需要
3. **假实现怎么切换？**
   - 配置字段（档 2）或环境变量（档 3）→ 合法
   - 硬编码调用（档 4）→ 升级为协议 + 注入/配置
   - 业务代码里的环境分支（档 5）→ 反模式，重写

### 档 2 和档 3 怎么选

- 会只想替换**某一个**组件（只假 LLM、不假短信）→ **档 2**（每个假实现有自己的配置字段，可逐个验证）
- 想"假就全假"（环境级开关）、假实现 ≥5 个 → **档 3**（一个环境变量管一批）
- 两档都必须**快速失败**：生产环境选中假实现必须启动即报错（如 `env=prod and provider=mock → raise`），不能静默运行

## 二、替身叫什么：Meszaros 五类命名

判断替身属于哪一类，一条主链：**替身有没有"自己的逻辑"？** → 有 → Fake；没有 → **测试跑完验证什么？** → 验证结果 → Stub；验证调用 → Spy（事后查）/ Mock（预先声明）；什么都不验证 → Dummy。

| 类型 | 做什么 | 怎么验证 | 什么时候会坑你 |
|---|---|---|---|
| **Dummy** | 只占参数位，从不用 | 无 | "以为没用到"其实是用了 → 配 `_Never*`（一碰就炸）验证"不该发生的调用确实没发生" |
| **Stub** | 固定返回某个值（喂间接输入） | 无，最后验证被测代码的结果 | 固定值和真实差异太大 → 测试绿、真实环境炸 |
| **Spy** | Stub + 记录每次调用 | 事后查记录（`spy.calls == [...]`） | 查得太细 → 耦合实现细节，重构必碎 |
| **Mock** | 预先声明期望（"必须这样调用"） | 期望不满足即失败 | 用太多 → 锁死实现细节，重构必碎 |
| **Fake** | 简化但能真跑的实现（有自己的逻辑） | 无 | 逻辑和真实实现漂移（最贵，要维护） |

### 各类替身的使用频率（不是每类都要写）

- **主力（~90%）**：Fake——产品侧假实现和测试侧有逻辑的替身
- **常用**：Stub（固定返回）、Spy（记调用记录）——可编程替身的两半
- **边缘且有替代**：Dummy 配 `_Never*` 强化（验证不该发生的调用）；Mock 交给 unittest.mock 的 `assert_called_*`，手写替身一般不写
- 默认写 Fake/Stub/Spy；Dummy/Mock 出现时走替代路径，不新造类

### 命名规则（名字前缀直接说明角色）

- `_Stub*`：喂数据的（固定返回）
- `_Spy*`：要查调用记录的（记 calls）——手写替身要验证"被怎么调用"时默认用 Spy（事后查），重构时不脆
- `_Fake*`：有自己逻辑的（能真跑）
- `_Never*`：不许碰的（碰即炸）——即"期望绝不调用"的 Mock 特例
- `_Mock*`（预先声明期望、自动验证）：**手写替身一般不写**；Mock 的用法由 `_Never*`（验证不该调用）与 unittest.mock 的 `assert_called_*`（框架工具）承担
- **产品侧假实现（档 2/3）统一 `Fake*` 命名**（如 `FakeLLMClient`），禁用 `Mock*`——"Mock"一词仅指 unittest.mock 框架工具与 `_Never*`，避免角色混淆

**下划线即文件内私有**：带 `_` 前缀 = 只在这个测试文件里用（file-local）；共享替身不带 `_`，用全名（如 `FakePromptLoader`，与 testkit 现有 `build_*` 命名一致）。

### 复用与放置规则

1. **测试要替身，先看产品侧有没有现成的假实现**（档 2 的假实现）——够用就复用（如 `Fake*Client(output=...)` 就能编程输出），不够再自造（`AsyncMock(wraps=...)` 增强）
2. **测试工具放哪，跟着被测代码的域走**：跨域通用 → `tests/testkit/`（现有 `db/`、`helper/`、`builders/` 不动）；域内专用 → `tests/<域>/testkit/`（AI 域替身进 `tests/ai/testkit/`，AI 域自己管）
3. **共享判定**：被 ≥2 个测试文件用 → 共享（进 testkit，不带 `_`）；只用 1 个 → 留文件内（带 `_`）
4. 用 unittest.mock 必须加 `spec=`（防止拼错属性名时静默产生假对象，而不是立刻报错）

## 三、反模式禁令（写死，不许犯）

1. **测试劫持生产行为**：业务代码里出现"测试"语义（`if is_test` / 测试专用环境变量分支 / 为测试改 TTL/限额）→ 反模式。历史实证：git `128c555` 清理了订单预留 TTL 的环境变量分支（测试曾用环境变量把 TTL 调短），替代方案是测试侧直接回拨订单过期时间（`backdate_order_expires_at`）——生产逻辑走正常路径，测试自己造数据状态。
2. **硬编码假实现**：业务代码直接调用假实现（假短信的 `send` 写死在业务函数里）→ 升级为协议 + 构造注入/配置切换（档 2）。
3. **环境分支的理由判据**：环境分支的理由是"环境本身就该不同"（日志格式、DEBUG、数据库地址——合法配置）还是"为了测试"（劫持——反模式）？看理由，不看形态。

## 四、决策流程（写替身前走一遍）

```
① 这个依赖能在本地跑起来吗？ → 能 → 测试用真实的（不需要替身）
② 不能 → 产品选了"开箱即用"（不配凭据也能跑）吗？
   ├─ 选了 → 档 2（每个组件一个配置字段）或档 3（环境变量管一批）+ 快速失败
   └─ 没选 → 替身全在 tests/
③ 这个替身：有自己逻辑 → Fake；固定返回 → Stub；要查调用记录 → Spy；预先声明期望 → Mock；只占位 → Dummy
④ 产品侧有现成假实现吗？ → 有 → 复用；没有 → 自造（共享进 testkit）
```
