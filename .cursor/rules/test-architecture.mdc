---
description: 测试四层架构、SAVEPOINT 事务隔离、integration_client 约定与 fail-fast fixture
globs: tests/**/*.py,tests/conftest.py,.cursor/rules/test-architecture.mdc
alwaysApply: false
---

# 测试架构纪律

OpenSpec：`test-architecture-v4`。
旧反模式对照：[docs/troubleshooting/测试架构-旧模式反模式记录.md](../../docs/troubleshooting/测试架构-旧模式反模式记录.md)

## 四层职责

| 层 | 位置 | 职责 |
|----|------|------|
| **Case** | `tests/**/test_*.py` | Assert-First；Act 在 Case 可见；主体是 assert |
| **Fixture** | `tests/conftest.py` | 生命周期 + 注入瘦身 `*Context`；Setup **fail-fast** |
| **Support** | `tests/support/` | helper（`auth.py`/`catalog.py`/`ordering.py`）、`build_*`、`*Result`、DB 断言（`db/`） |
| **Utilities** | `tests/support/utils.py` | `bearer_headers`、`bootstrap_test_env` 等纯函数，无 HTTP/DB |

## Case 层：禁止

```python
# ✗ test 文件内可复用 HTTP helper
async def _create_product(client, ...): ...

# ✗ test 互 import
from tests.catalog.test_create_product import _create_product

# ✗ test 内 DB seed
async def _seed_inactive_user(database_url, ...): ...

# ✗ test 内 import app.*.repository
from app.ordering.repository import OrderRepository
```

**允许**：`test_*` 函数、模块级常量；`tests/unit/**` 内 file-local baseline（如 `_valid_*_kwargs`），且 **不** import support。

Case 参数 **MUST** 注解（如 `integration_client: AsyncClient`）。简单 GET 的 Act **MAY** inline `integration_client.get` + `model_validate`。

## integration_client 与事务隔离

`@pytest.mark.integration` 且发 HTTP 写库的测试 **MUST** 使用 `integration_client` fixture。
`integration_client` 依赖 `db_session`，通过 `dependency_overrides[get_db]` 使 HTTP 请求走 SAVEPOINT 测试 session。
测试 teardown 时外层 ROLLBACK，零数据残留。

| Fixture | `get_db` override | 用途 |
|---------|-------------------|------|
| `integration_client` | ✓ 已注册 | `@integration` HTTP 业务测 |
| `client` | ✗ 无 | health / readiness / 探针例外 |

**探针例外**（不使用 `integration_client`）：
- `tests/ops/test_readiness.py`、`tests/ops/test_health.py`
- `tests/catalog/test_admin_seed.py`（独立 engine 查 alembic seed）
- `tests/user/test_user_summary.py`（纯 service，无 HTTP）

## Support：helper 与 orchestrator

helper 按域拆分，位于 `tests/support/helper/`：
- `tests/support/helper/auth.py` — `register_user`、`login_user`、`login_admin`、`register_authenticated`、`auth_headers`
- `tests/support/helper/catalog.py` — `create_shop`、`register_and_open_shop`、`create_category`、`create_product`
- `tests/support/helper/ordering.py` — `create_order`、`create_order_by_seller`、`pay_order`、`create_shipment`、`confirm_receipt`、`cancel_order`、`arrange_purchasable_product`、`arrange_confirmed_order`
- `tests/support/db/` — DB 状态断言（`backdate_order_expires_at`、`get_product_stock` 等）

**原子 helper**（一次 HTTP）→ 单步 `*Result`（`status_code`、`body`、可选 `request`）：

```python
# ✓ 如实返回，不 assert 成功
async def register_user(client: AsyncClient, ...) -> RegisterResult:
    response = await client.post("/auth/register", ...)
    return RegisterResult(status_code=response.status_code, body=..., ...)

# ✗ helper 内替 Case 判定
assert response.status_code == 201
```

**禁止**：helper 接收/返回 `*Context`；helper 内 `assert status_code == 201`。

**Orchestrator 返回值**：

| 场景 | 返回 |
|------|------|
| 多步 HTTP（如注册→开店） | `tuple[RegisterResult, ShopResult \| None]` |
| 扇入后单步新 HTTP（如 `create_product`） | `ProductResult` 等单步 `*Result` |
| ad-hoc 组合 dataclass | **禁止** |

扇入 orchestrator 的前置 **MUST** 仍在 Case 作用域（Context 字段、显式参数）；**禁止** one-shot 黑盒。

## Context 与 Fixture

```python
@dataclass(frozen=True)
class AuthContext:
    access_token: str
    email: str

@dataclass(frozen=True)
class AdminAuthContext:
    access_token: str

@dataclass(frozen=True)
class ShopOwnerContext:
    access_token: str
    shop_id: str
    email: str
```

- Context 仅持有直接字段（`access_token`、`shop_id`、`email`），**不持有** PipelineResult 或 headers 拷贝
- `bearer_headers(access_token: str)` / `auth_headers(access_token: str)` 接受纯字符串 token，位于 `tests/support/utils.py`
- Setup fixture 失败 → `pytest.fail`；失败场景在 Case 内调 helper assert
- Setup fixture 依赖 `integration_client`（确保 SAVEPOINT 作用域）

```python
# ✓ admin_auth_headers 风格：fail-fast → 瘦身 Context
logged_in = await login_user(integration_client, email=..., password=...)
if logged_in.status_code != 200:
    pytest.fail("...")
return AdminAuthContext(access_token=logged_in.body.access_token)

# ✗ 透传失败 Context（无 fail-fast）
return await register_and_open_shop(client)
```

## Arrange / Act 铁律

- **被测 API 以外皆为 Arrange**（fixture 内 `register_user` + `create_shop` 仍是 Arrange）
- Setup fixture **MUST NOT** 替 Case 执行被测端点
- Act 留在 Case：inline HTTP 或 Act 型 helper；Arrange 可厚，在 support/fixture

## 目录

- 业务 integration：`tests/{domain}/test_*.py`
- schema 单测：`tests/unit/{domain}/`（不 import support）
- 运维探针：`tests/ops/`（health、readiness、migration smoke）
- helper 实现：`tests/support/helper/{auth,catalog,ordering}.py`
- DB 断言：`tests/support/db/{ordering,catalog}.py`

## 生成/审查测试时

1. 新 helper → `tests/support/{domain}.py`，不写入 `test_*.py`
2. 跨步编排 → tuple 或显式 `*Result` 传参，不返胖 Context
3. 新 fixture → fail-fast + 瘦身 `*Context(access_token=..., ...)`
4. 写库测试 → `integration_client` + `db_session`（探针例外除外）
5. 懒释放/过期 → `tests/support/db` backdate，不设 env TTL 或 sleep
6. **禁止** 在 `scripts/` 新增 `*_curl_smoke.sh` 或与 integration 重复的 bash HTTP 编排；本地/CI 验收用 `task ci` / `task test`
