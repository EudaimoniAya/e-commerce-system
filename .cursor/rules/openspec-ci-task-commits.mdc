---
description: OpenSpec 默认不 commit/push；Task 聚焦本地测试与远程 CI 时自动 commit+push
globs: openspec/changes/**/tasks.md,.github/workflows/**
alwaysApply: false
---

# OpenSpec：提交与 Push 约定

## Commit 粒度（自 test-architecture-v4 起）

| 层级 | 标识 | 用途 |
|------|------|------|
| **Task 节** | `tasks.md` 的 `## N` | **默认一 commit 单位** |
| **子项** | `- [ ] N.x` | apply 勾选粒度；**默认不单独 commit** |

- **默认**：完成一整节（勾选该节全部子项）后 **一次 commit**；message 用 `Task N` 或 `Task N ~N.x` 概括。
- **例外**（须在 change 的 `tasks.md` 或 design 中写明）：POC 门禁（如 §3.0）、§6 CI 与 spec sync 拆分、CI 双提交（见下）。
- 废止旧习惯：按 **1–2 个子项（N.x）** 一切一 commit；除非用户当次 apply 明确要求细粒度提交。

## 默认

- Agent **只改代码/文档/tasks**，**不** `git commit`，**不** `git push`。
- 完成后提供符合项目格式的 **建议 commit message**，由 **用户自行提交**。
- 用户未明确要求时，**禁止**替用户 commit 或 push。

## 例外：Task 聚焦「本地测试 & 远程 CI」

**与 tasks.md 第几节无关**，看 **当前 Task 描述是否聚焦** 本地验证 + 远程 CI。命中时 Agent **自动 commit + push**（无需用户重复提醒）。

### 判定信号（满足其一即可视为 CI Task）

- 本地：`task migrate`、`task ci` / `task test`、DoD 本地全绿
- 远程：`.github/workflows/test.yaml`、`JWT_SECRET_KEY` 等 CI env、`git push`、`GitHub Actions`、`workflow_dispatch`、`gh workflow run`、`gh run watch`
- 文案：`本地验证`、`远程 CI`、`确认 CI 全绿`、`push 并确认`

### 两次 commit / push（推荐，与 user-auth 范例一致）

**第一次：本地验证 + CI 配置**

1. 完成 Task（改 ci.yml、跑 migrate + task ci、必要时域测试）。
2. **commit + push**。

```text
ci(<scope>) [<short-change>]: Task <编号> <简短标题>

- <本地 migrate + task ci 结果>
- <关键验证（域 test 或 integration 覆盖）>
- ci.yml <具体 env 变更>；更新 tasks.md
```

**第二次：远程 CI 全绿确认**

1. `gh workflow run "Run Tests" --ref <branch>`（feature 分支无 PR 触发时）或确认 PR CI。
2. `gh run watch <run-id> --exit-status` 等待 **success**。
3. **再 commit + push**（勾选 tasks、附 Actions URL）。

```text
ci(<scope>) [<short-change>]: Task <编号> 确认 GitHub Actions CI 全绿

workflow_dispatch on <branch>: <run-url>
```

若用户将「本地验证」与「远程确认」写在 **同一个 Task** 里，可单次 commit+push，但须等 CI 全绿后再勾选完成。

## 通用 commit message 格式

```text
<type>(<scope>) [<short-change>]: Task <编号> <简短主题>

- <要点 1>
- <要点 2>
```

- **type**：`feat` / `test` / `ci` / `docs` / `build` 等
- **scope**：**受影响的业务域**（如 `ordering`、`engagement`）；跨域时写 `(catalog, engagement)`
- **`[<short-change>]`**：OpenSpec change 短标签（如 `engagement-browse` → `[browse]`）；规则见 ADR-008 决策 6
- **标题宜短**（≤72 字符）；细节放正文 `-` 列表，中文简体

## 禁止

- 对 **非 CI 聚焦** Task 自动 commit 或 push（如纯实现、文档、archive）。
- 在远程 CI 未绿前勾选「确认 CI 全绿」类 Task 或宣称 DoD 完成。
- 把「CI 配置变更」与「CI 全绿确认」混在一个 commit 里（除非用户明确要求单次提交）。

## 参考

- 范例：`test(engagement) [browse]: Task 1 TDD 红` / `ci(engagement) [browse]: Task 6 确认 CI 全绿`（CI 子项可单独 commit；节号作 message 编号，**非** CI 触发条件）
- DoD：README §Definition of Done
